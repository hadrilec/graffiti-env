stages:
    - build

variables:
  CONTAINER_RELEASE_API: "docker-registry.beta.innovation.insee.eu/xlapdo/image_dataviz_conj"

package:
  # Use stage build here, so the pages job may later pickup the created site.
  stage: build
  tags:
    - maven
  script:
    - 'docker build -t $CONTAINER_RELEASE_API .'
    - 'docker push $CONTAINER_RELEASE_API'
  only:
    - master@xlapdo/image_dataviz_conj
